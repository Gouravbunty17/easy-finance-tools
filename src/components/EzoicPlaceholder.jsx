// Deprecated compatibility shim. Ezoic monetization is paused while the site
// is not approved, so this re-export intentionally renders nothing.
// Safe to delete once no imports reference it.
export { default } from "./EzoicAd";
